services:
  ops-pilot:
    build: .
    container_name: ops-pilot
    restart: unless-stopped
    ports:
      - "5174:5174"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=5174
      - HOST=0.0.0.0
      - DATABASE_URL=file:/app/data/ops-pilot.db
